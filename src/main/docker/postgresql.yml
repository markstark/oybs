version: '2'
services:
  oybs-postgresql:
    image: postgres:12.3
    # volumes:
    #     - ~/volumes/jhipster/oybs/postgresql/:/var/lib/postgresql/data/
    environment:
      - POSTGRES_USER=oybs
      - POSTGRES_PASSWORD=
      - POSTGRES_HOST_AUTH_METHOD=trust
    ports:
      - 5432:5432
